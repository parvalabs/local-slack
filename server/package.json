{
  "name": "@local-slack/server",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "local-slack": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun --watch src/cli.ts",
    "start": "bun src/cli.ts",
    "test": "bun test",
    "build:binary": "bun build --compile --minify src/cli.ts --outfile ../local-slack"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
